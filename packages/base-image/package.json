{
  "name": "@iso/base-image",
  "version": "0.1.1",
  "private": true,
  "description": "Builds the iso 'base' image rootfs (vanilla npm + a thin environmental overlay) into ~/.iso/base/.staging",
  "type": "module",
  "main": "./build.mjs",
  "files": [
    "build.mjs",
    "sh"
  ],
  "engines": {
    "node": ">=22"
  }
}
